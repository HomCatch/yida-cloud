const user = {
    state: {
        // token: '',
        // leftNav: [],        // 左侧菜单项
        // topNav: [],         // 顶部菜单项
        // currentNav: '',     // 当前目录
        // funcs: [],          // 当前菜单功能按钮
    },
    mutations: {
        // setToken(state, token) {
        //     state.token = token;
        // }
    },
    actions: {
        // getNavList({ commit }) {
            
        // }
    }
}

export default user